import React from "react";

export default function NextDay() {
  return (
    <div className="m-auto container">
      test
      {/* <h1> {weatherData.title}</h1>
      <p>{weatherData.publicTimeFormatted}</p>
      {weatherData.forecasts.map(
        (forecast): JSX.Element => (
          <div key={forecast.date} className="flex space-x-2 justify-center items-center">
            <h2>{forecast.dateLabel}</h2>
            <p>{forecast.telop}</p>
            <p>最高気温:{forecast.temperature.max.celsius}度</p>
            <p>風向き:{forecast.detail.wind}</p>
            {forecast.detail.wave ? <p>波:{forecast.detail.wave}</p> : null}
            <img src={forecast.image.url} alt="weather" />
          </div>
        )
      )}
      <p>{weatherData.description.bodyText}</p> */}
    </div>
  );
}
